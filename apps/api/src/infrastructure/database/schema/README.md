# Schema PostgreSQL

As tabelas Drizzle serão adicionadas aqui somente com requisitos e políticas RLS aprovados. Migrações geradas ficam em `apps/api/drizzle` e devem ser revisadas antes de execução.
